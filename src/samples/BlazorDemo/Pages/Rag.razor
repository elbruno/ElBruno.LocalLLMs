@page "/rag"
@rendermode InteractiveServer

<PageTitle>RAG Playground — LocalLLMs Demo</PageTitle>

<h1>🔍 RAG Playground</h1>
<p>
    Add documents, then run semantic search against them using the
    <code>LocalRagPipeline</code>. Retrieved chunks are ranked by similarity.
</p>

<RagPlayground />

<CodeSample Code="@CodeSnippets.RagPlayground" />

@code { }
